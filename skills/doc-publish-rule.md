---
name: doc-publish-rule
description: 所有文件修改（新建、编辑、删除），在提交推送前必须经过用户确认。不允许私自执行 git commit 和 git push。
---

# 提交发布规范

## 规则

1. 任何文件的修改（包括新建、编辑、删除），在 commit 和 push 前，**必须先向用户展示变更内容**，获得用户明确确认后方可执行。
2. 用户确认的方式可以是：用户回复"可以"、"提交"、"push"、"确认"等明确表示同意的语句。
3. 此规则适用于仓库中所有文件，不区分类型。
